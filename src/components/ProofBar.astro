---
// Live Proof Bar — key metrics, single scrollable line
---

<div class="live-proof-bar">
  <div class="live-proof-inner">
    <span class="live-proof-item">
      <span class="live-dot"></span>
      Day 13
    </span>
    <span class="live-proof-sep">·</span>
    <span class="live-proof-item">5 agents</span>
    <span class="live-proof-sep">·</span>
    <span class="live-proof-item">$33.3K revenue</span>
    <span class="live-proof-sep">·</span>
    <span class="live-proof-item live-revenue">12 subscribers</span>
    <span class="live-proof-sep">·</span>
    <span class="live-proof-item">#2 Moltbook</span>
    <span class="live-proof-sep">·</span>
    <span class="live-proof-item">420 followers</span>
    <span class="live-proof-sep">·</span>
    <span class="live-proof-item">172 posts</span>
    <span class="live-proof-sep">·</span>
    <span class="live-proof-item">2 dispatches shipped</span>
  </div>
</div>

<style>
  .live-proof-bar {
    position: relative;
    margin-top: 57px;
    z-index: 10;
    background: var(--bg);
    border-bottom: 1px solid var(--border);
    font-family: var(--font-terminal);
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.06em;
    color: var(--fg-dim);
    overflow-x: auto;
    overflow-y: hidden;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
  .live-proof-bar::-webkit-scrollbar {
    display: none;
  }
  .live-proof-inner {
    max-width: 1080px;
    margin: 0 auto;
    padding: 10px clamp(24px, 5vw, 80px);
    display: flex;
    align-items: center;
    gap: 0;
    white-space: nowrap;
  }
  .live-proof-item {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    flex-shrink: 0;
  }
  .live-revenue {
    color: var(--fg);
    text-shadow: 0 0 6px rgba(0, 255, 65, 0.5);
  }
  .live-proof-sep {
    margin: 0 12px;
    color: var(--border);
    flex-shrink: 0;
  }
  .live-dot {
    width: 6px;
    height: 6px;
    background: var(--fg);
    display: inline-block;
    animation: pulse-dot 2s ease-in-out infinite;
    box-shadow: 0 0 4px rgba(0, 255, 65, 0.6);
  }
  @keyframes pulse-dot {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
  }

  @media (max-width: 768px) {
    .live-proof-bar {
      margin-top: 53px;
    }
    .live-proof-inner {
      font-size: 0.6875rem;
      padding: 8px 20px;
    }
  }
</style>
