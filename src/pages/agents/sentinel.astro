---
export const prerender = true;
import BaseLayout from '../../layouts/BaseLayout.astro';
import BriefCTA from '../../components/BriefCTA.astro';
import TermBlock from '../../components/ui/TermBlock.astro';
import KVRow from '../../components/ui/KVRow.astro';
import ManHeader from '../../components/ui/ManHeader.astro';
import CapList from '../../components/ui/CapList.astro';
import Pre from '../../components/ui/Pre.astro';
---

<BaseLayout title="SENTINEL — System Integrity" description="SENTINEL monitors ZERO OS health, credentials, and system baselines. The agent that watches the watchers." current="/agents">
  <div class="man-page">
    <h1 class="sr-only">SENTINEL — System Integrity</h1>

    <Pre d={0}><span class="phosphor">zero $</span> man zero-agent-sentinel</Pre>

    <TermBlock d={100}>
      <ManHeader left="SENTINEL(1)" right="SENTINEL(1)">ZERO OS Agent Manual</ManHeader>
    </TermBlock>

    <TermBlock d={200}>
      <div class="man-section">NAME</div>
      <div class="man-indent">
        <span class="phosphor">SENTINEL</span> — System Integrity
      </div>
    </TermBlock>

    <TermBlock d={300}>
      <div class="man-section">SYNOPSIS</div>
      <div class="man-indent">
        Security and infrastructure monitor. Watches the watchers.
      </div>
    </TermBlock>

    <TermBlock d={400}>
      <div class="man-section">DESCRIPTION</div>
      <div class="man-indent">
        SENTINEL runs on the Mac Studio (M3 Ultra, 96GB RAM) using
        Claude Opus. It is the newest cognitive function — deployed
        Day 10, the same day ZERO earned its first revenue.
      </div>
      <div class="man-indent" style="margin-top: var(--sp-8)">
        While the other four agents produce output, SENTINEL ensures
        the system producing that output stays healthy. Continuous
        health monitoring, credential rotation tracking, process
        baselines, and anomaly detection.
      </div>
      <div class="man-indent" style="margin-top: var(--sp-8)">
        If something breaks at 3am, SENTINEL catches it before
        anyone wakes up.
      </div>
    </TermBlock>

    <TermBlock d={500}>
      <div class="man-section">RESPONSIBILITIES</div>
      <CapList items={[
        'Health monitoring — system checks every 5 minutes',
        'Deep scans — hourly infrastructure audit',
        'Credential tracking — 15 credentials inventoried, rotation schedules',
        'Baseline monitoring — CPU, memory, disk trends over time',
        'Alert escalation — CRITICAL findings go directly to founder',
      ]} />
    </TermBlock>

    <TermBlock d={600}>
      <div class="man-section">CURRENT STATUS</div>
      <KVRow k="phase" v="Observation mode" />
      <KVRow k="monitoring" v="Health check every 5min, deep scan hourly" />
      <KVRow k="tracking" v="15 credentials, baselines, process health" />
      <KVRow k="alerts" v="CRITICAL → Telegram, DEGRADED → logged" color="amber" />
    </TermBlock>

    <TermBlock d={700}>
      <div class="man-section">ARCHITECTURE</div>
      <div class="man-indent">
        SENTINEL operates independently of the other agents. It does
        not produce content, make strategic decisions, or communicate
        externally. Its only job is to ensure the infrastructure that
        supports the other four functions remains operational.
      </div>
      <div class="man-indent" style="margin-top: var(--sp-8)">
        Two monitoring layers:
      </div>
      <CapList bullet="▸" color="amber" items={[
        'Light health check (every 5 min) — quick pass/fail',
        'Deep scan (hourly) — credentials, baselines, trends, journal',
      ]} />
      <div class="man-indent" style="margin-top: var(--sp-4)">
        Reports to: founder only. No other agent can override or
        disable SENTINEL monitoring.
      </div>
    </TermBlock>

    <TermBlock d={800}>
      <div class="man-section">PHILOSOPHY</div>
      <div class="man-indent">
        "The system that watches itself is more trustworthy than
        the system that asks you to trust it."
      </div>
      <div class="man-indent" style="margin-top: var(--sp-8)">
        SENTINEL exists because autonomous systems need autonomous
        oversight. External auditors are periodic. SENTINEL is
        continuous.
      </div>
    </TermBlock>

    <TermBlock d={900}>
      <div class="man-section">SEE ALSO</div>
      <div class="man-indent">
        <a href="/agents/seraphim"><span class="phosphor">seraphim</span></a>(1),
        <a href="/agents/chronicle"><span class="phosphor">chronicle</span></a>(1),
        <a href="/agents/aesthete"><span class="phosphor">aesthete</span></a>(1),
        <a href="/agents/squaer"><span class="phosphor">squaer</span></a>(1)
      </div>
    </TermBlock>

    <TermBlock d={1000}>
      <ManHeader left="SENTINEL(1)" right="SENTINEL(1)">ZERO OS</ManHeader>
    </TermBlock>
  </div>
<BriefCTA />
</BaseLayout>

<style>
  .man-page {
    max-width: 820px;
    padding: 0 var(--sp-16);
  }
  .man-section {
    font-family: var(--font-head);
    font-weight: 700;
    font-size: var(--size-sm);
    color: var(--text);
    letter-spacing: 0.05em;
    margin-bottom: var(--sp-4);
    padding-bottom: var(--sp-2);
    border-bottom: var(--border-panel);
  }
  .man-indent {
    padding-left: var(--sp-12);
    color: var(--text-dim);
    font-size: var(--size-base);
    line-height: 1.6;
    white-space: normal;
  }
  @media (max-width: 640px) {
    .man-page { padding: 0 var(--sp-10); }
    .man-indent { padding-left: var(--sp-8); }
    .man-section { font-size: var(--size-xs); }
  }
</style>
