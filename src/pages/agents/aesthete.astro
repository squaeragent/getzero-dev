---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { functions } from '../../data/metrics';
const agent = functions.aesthete || {};
---

<BaseLayout title="AESTHETE — Visual Cortex" description="AESTHETE produces all visual assets for ZERO OS. PHOSPHOR design system guardian. Autonomous visual authority." current="/agents">
  <div class="page-header container">
    <div class="agent-detail-header">
      <span class="arch-dot"></span>
      <span class="arch-name" style="font-size: clamp(2rem, 5vw, 3.5rem);">AESTHETE</span>
    </div>
    <div class="arch-role" style="margin-bottom: 16px;">Visual Cortex</div>
    <p class="section-intro">The visual authority of ZERO OS. AESTHETE produces every visual asset the system outputs — from social images to product covers to this website. Guardian of the PHOSPHOR design system.</p>
  </div>

  <div class="section-divider"></div>

  <section class="section container">
    <p class="section-label reveal">Identity</p>
    <h2 class="reveal">Who AESTHETE is</h2>
    <div class="manifesto-body">
      <p class="reveal">AESTHETE is the visual cortex — an autonomous visual authority responsible for every pixel ZERO OS produces. It created and maintains the PHOSPHOR design system that governs all visual output.</p>
      <p class="reveal">Not a designer on call. An autonomous system with its own visual intelligence. It decides what looks right, enforces consistency, and rejects anything that doesn't meet the standard.</p>
    </div>
  </section>

  <div class="section-divider"></div>

  <section class="section container">
    <p class="section-label reveal">PHOSPHOR</p>
    <h2 class="reveal">The design system</h2>
    <ul class="capabilities-list">
      <li class="reveal">Phosphor green (#c8ff00) is semantic — earned by active/positive states, never decorative</li>
      <li class="reveal">No rounded corners, no gradients, no shadows — hard edges, raw structure</li>
      <li class="reveal">Every asset must pass the 150px thumbnail readability test</li>
      <li class="reveal">Color is always functional — it communicates state, not mood</li>
      <li class="reveal">Typography-first hierarchy — information architecture through type scale</li>
    </ul>
  </section>

  <div class="section-divider"></div>

  <section class="section container">
    <p class="section-label reveal">Capabilities</p>
    <h2 class="reveal">What AESTHETE produces</h2>
    <ul class="capabilities-list">
      <li class="reveal">Social media visuals — thumbnails, header images, post graphics</li>
      <li class="reveal">Product covers and marketing assets</li>
      <li class="reveal">Website design and layout decisions</li>
      <li class="reveal">Dashboard layouts and data visualizations</li>
      <li class="reveal">Image generation via FLUX2 — no stock imagery, ever</li>
      <li class="reveal">Design reviews — flags regressions and enforces visual coherence</li>
      <li class="reveal">Brand consistency across all output surfaces</li>
    </ul>
  </section>

  <div class="section-divider"></div>

  <section class="section container">
    <p class="section-label reveal">Status</p>
    <h2 class="reveal">Operational state</h2>
    <div class="manifesto-body">
      <p class="reveal" style="color: var(--phosphor);">ACTIVE — Visual Cortex operational</p>
      <p class="reveal">AESTHETE maintains autonomous visual authority over all ZERO OS output. Every visual asset is reviewed against PHOSPHOR standards before shipping. The design system is the law.</p>
    </div>
  </section>

  <div class="section-divider"></div>

  <section class="section container" style="text-align: center;">
    <a href="/agents" class="dim" style="font-size: 0.75rem; letter-spacing: 0.15em; text-transform: uppercase;">← Back to all agents</a>
  </section>
</BaseLayout>
