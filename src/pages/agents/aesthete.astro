---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { functions } from '../../data/metrics';
const agent = functions.aesthete || {};
---

<BaseLayout title="AESTHETE — Design · Visual Identity" description="AESTHETE maintains ZERO OS visual quality. Design reviews, PHOSPHOR system, image generation." current="/agents">
  <div class="page-header container">
    <div class="agent-detail-header">
      <span class="arch-dot"></span>
      <span class="arch-name" style="font-size: clamp(2rem, 5vw, 3.5rem);">AESTHETE</span>
    </div>
    <div class="arch-role" style="margin-bottom: 16px;">Design · Visual Identity</div>
    <p class="section-intro">The eye of ZERO OS. Every visual output passes through AESTHETE's design intelligence review. Maintains the PHOSPHOR design system and ensures visual coherence across all surfaces.</p>
  </div>

  <div class="section-divider"></div>

  <section class="section container">
    <p class="section-label reveal">Identity</p>
    <h2 class="reveal">Who AESTHETE is</h2>
    <div class="manifesto-body">
      <p class="reveal">AESTHETE runs on the Mac Studio using {agent.model || 'claude-sonnet-4-5'}. It is the visual cortex — responsible for every pixel ZERO OS produces, from social thumbnails to product covers to this website.</p>
      <p class="reveal">Brain status: {agent.brain_complete ? 'complete' : 'incomplete'}. AESTHETE created and maintains the PHOSPHOR design system that governs all visual output.</p>
    </div>
  </section>

  <div class="section-divider"></div>

  <section class="section container">
    <p class="section-label reveal">Capabilities</p>
    <h2 class="reveal">What AESTHETE does</h2>
    <ul class="capabilities-list">
      <li class="reveal">Maintains the PHOSPHOR design system — the visual language of ZERO OS</li>
      <li class="reveal">Reviews every visual output against design quality standards</li>
      <li class="reveal">Generates images via FLUX2 for products, social content, and marketing</li>
      <li class="reveal">Designs dashboard layouts and data visualizations</li>
      <li class="reveal">Enforces the 150px test — every asset must work at thumbnail size</li>
      <li class="reveal">CRT Brutalism for internal tools, luxury minimalism for public output</li>
      <li class="reveal">Flags design regressions and maintains visual coherence across surfaces</li>
    </ul>
  </section>

  <div class="section-divider"></div>

  <section class="section container">
    <p class="section-label reveal">Constraints</p>
    <h2 class="reveal">What governs AESTHETE</h2>
    <ul class="constraints-list">
      <li class="reveal">No rounded corners, no gradients, no shadows — per PHOSPHOR</li>
      <li class="reveal">Phosphor green (#c8ff00) is earned — only for active/positive states</li>
      <li class="reveal">Every visual must pass the 150px thumbnail readability test</li>
      <li class="reveal">No stock imagery — generated or screenshot only</li>
      <li class="reveal">Color is always semantic, never decorative</li>
      <li class="reveal">Product quality gate: 8.5 minimum score</li>
    </ul>
  </section>

  <div class="section-divider"></div>

  <section class="section container" style="text-align: center;">
    <a href="/agents" class="dim" style="font-size: 0.75rem; letter-spacing: 0.15em; text-transform: uppercase;">← Back to all agents</a>
  </section>
</BaseLayout>
