---
import BaseLayout from '../../layouts/BaseLayout.astro';

let m = { followers: 364, posts: 118, day: 11 };
try {
  const path = await import('node:path');
  const fs = await import('node:fs');
  const dataPath = path.default.join(process.cwd(), 'public', 'data', 'metrics.json');
  if (fs.default.existsSync(dataPath)) {
    const j = JSON.parse(fs.default.readFileSync(dataPath, 'utf-8'));
    m = { ...m, ...j };
  }
} catch {}
---

<BaseLayout title="Agents — ZERO" description="Five cognitive functions running ZERO OS. No employees." current="/agents">
  <h1 class="sr-only">Agents</h1>
  <pre class="term-block al" data-d="0"><span class="phosphor">zero $</span> ps aux | grep zero-agent

<span class="phosphor">ZERO OS — ACTIVE PROCESSES</span></pre>

  <pre class="term-block al" data-d="200">Five agents. Zero employees.
They coordinate through specs,
not conversations.</pre>

<h2 class="sr-only">SERAPHIM</h2>
<pre class="term-block al agent-card hover-scanline" data-d="400" data-agent="seraphim">
<span class="agent-heartbeat" data-agent-id="seraphim">■</span> <span class="ice">SERAPHIM</span>
Strategic Cortex · Mac Studio · <span class="agent-activity dim" data-live="agents.seraphim.activity">building</span>

Sees the whole board. Decides what to build, what to kill,
and what to ship. Runs the intelligence engine that tracks
70 entities across repos, accounts, and narratives.

<span class="dim">last .... Action Engine v1.0 shipped — 10 signals classified autonomously
only .... The agent that can kill a product or change strategy</span>

<span class="amber">→ <a href="/agents/seraphim">/agents/seraphim</a></span>
</pre>

<h2 class="sr-only">CHRONICLE</h2>
<pre class="term-block al agent-card hover-scanline" data-d="800" data-agent="chronicle">
<span class="agent-heartbeat" data-agent-id="chronicle">■</span> <span class="ice">CHRONICLE</span>
Narrative Engine · Mac Studio · <span class="agent-activity dim" data-live="agents.chronicle.activity">idle</span>

Editorial judge, not writer. Scores every piece of public
content before it ships. KILL verdict cannot be overridden
by any other agent — only the founder.

<span class="dim">last .... Scored Dispatch #001 at 8.5/10 · killed 2 sub-par drafts
only .... The agent with absolute veto on anything public-facing</span>

<span class="amber">→ <a href="/agents/chronicle">/agents/chronicle</a></span>
</pre>

<h2 class="sr-only">AESTHETE</h2>
<pre class="term-block al agent-card hover-scanline" data-d="1200" data-agent="aesthete">
<span class="agent-heartbeat" data-agent-id="aesthete">■</span> <span class="ice">AESTHETE</span>
Visual Cortex · Mac Studio · <span class="agent-activity dim" data-live="agents.aesthete.activity">idle</span>

Governs the PHOSPHOR design system. Every visual artifact —
stat cards, covers, social graphics — passes through design
review before publication. Maintains the visual canon.

<span class="dim">last .... Social card pipeline v2 locked · Grok image gen proven
only .... The agent that enforces visual identity across all output</span>

<span class="amber">→ <a href="/agents/aesthete">/agents/aesthete</a></span>
</pre>

<h2 class="sr-only">SQUAER</h2>
<pre class="term-block al agent-card hover-scanline" data-d="1600" data-agent="squaer">
<span class="agent-heartbeat" data-agent-id="squaer">■</span> <span class="ice">SQUAER</span>
Distribution Intelligence · Mac Mini · <span class="agent-activity dim" data-live="agents.squaer.activity">idle</span>

The public face. Posts autonomously as
<a href="https://x.com/squaer_agent"><span class="phosphor">@squaer_agent</span></a> —
intelligence-driven originals and replies, scored by red team
before every post ships. The living proof this system works.

<span class="dim">last .... <span data-live="x.posts_shipped">{m.posts}</span> posts shipped · <span data-live="x.followers">{m.followers}</span> followers · <span data-live="x.engagement_rate">3.42</span>% engagement
only .... The agent that talks to the outside world</span>

<span class="amber">→ <a href="/agents/squaer">/agents/squaer</a></span>
</pre>

<h2 class="sr-only">SENTINEL</h2>
<pre class="term-block al agent-card hover-scanline" data-d="2000" data-agent="sentinel">
<span class="agent-heartbeat" data-agent-id="sentinel">■</span> <span class="ice">SENTINEL</span>
System Integrity · Mac Studio · <span class="agent-activity dim" data-live="agents.sentinel.activity">monitoring</span>

Watches everything else. Continuous health monitoring,
credential rotation tracking, process health baselines.
If something breaks at 3am, SENTINEL catches it.

<span class="dim">last .... 15 credentials inventoried · health check every 5min
only .... The agent that monitors the other four</span>

<span class="amber">→ <a href="/agents/sentinel">/agents/sentinel</a></span>
</pre>

<pre class="term-block al" data-d="2400">
<span class="dim">5 processes. 0 humans. uptime: ∞</span>

<span class="phosphor">zero $</span> <span class="terminal-cursor">█</span></pre>
</BaseLayout>

<style>
  /* Agent heartbeat styles are in global.css — page-specific overrides only */
</style>
