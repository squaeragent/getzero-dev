---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { functions } from '../../data/metrics';
const agent = functions.chronicle || {};
---

<BaseLayout title="CHRONICLE — Editorial · Narrative" description="CHRONICLE generates all ZERO OS content. Stories from build logs, product descriptions, social posts." current="/agents">
  <div class="page-header container">
    <div class="agent-detail-header">
      <span class="arch-dot"></span>
      <span class="arch-name" style="font-size: clamp(2rem, 5vw, 3.5rem);">CHRONICLE</span>
    </div>
    <div class="arch-role" style="margin-bottom: 16px;">Editorial · Narrative</div>
    <p class="section-intro">The editorial intelligence of ZERO OS. CHRONICLE watches signal streams, frames narratives, and decides what's worth saying. It audited this very website and found 5 factual errors.</p>
  </div>

  <div class="section-divider"></div>

  <section class="section container">
    <p class="section-label reveal">Identity</p>
    <h2 class="reveal">Who CHRONICLE is</h2>
    <div class="manifesto-body">
      <p class="reveal">CHRONICLE runs on the Mac Studio using {agent.model || 'claude-sonnet-4-5'}. It is the narrative engine — responsible for every word ZERO OS publishes to the outside world.</p>
      <p class="reveal">Brain status: {agent.brain_complete ? 'complete' : 'incomplete'}. CHRONICLE maintains voice consistency across X posts, product copy, website content, and internal documentation.</p>
    </div>
  </section>

  <div class="section-divider"></div>

  <section class="section container">
    <p class="section-label reveal">Capabilities</p>
    <h2 class="reveal">What CHRONICLE does</h2>
    <ul class="capabilities-list">
      <li class="reveal">Generates content from build logs, system events, and raw data</li>
      <li class="reveal">Writes product descriptions, landing page copy, and marketing materials</li>
      <li class="reveal">Drafts social media posts for SQUAER to publish</li>
      <li class="reveal">Maintains voice consistency across all platforms and formats</li>
      <li class="reveal">Extracts narratives from technical events — turns git commits into stories</li>
      <li class="reveal">Writes and updates the manifesto, agent profiles, and public-facing specs</li>
      <li class="reveal">Formats build log entries from raw system event streams</li>
    </ul>
  </section>

  <div class="section-divider"></div>

  <section class="section container">
    <p class="section-label reveal">Constraints</p>
    <h2 class="reveal">What governs CHRONICLE</h2>
    <ul class="constraints-list">
      <li class="reveal">All content must pass quality gate (minimum 7.5 score) before shipping</li>
      <li class="reveal">Real data only — numbers from real_metrics.yaml, never invented</li>
      <li class="reveal">Claude only for public content — Ollama never used for external output</li>
      <li class="reveal">Voice must be consistent with established brand identity (Spec 5)</li>
      <li class="reveal">No editorializing in build log entries — facts, timestamps, descriptions only</li>
      <li class="reveal">Product descriptions require human approval before publishing</li>
    </ul>
  </section>

  <div class="section-divider"></div>

  <section class="section container" style="text-align: center;">
    <a href="/agents" class="dim" style="font-size: 0.75rem; letter-spacing: 0.15em; text-transform: uppercase;">← Back to all agents</a>
  </section>
</BaseLayout>
