---
export const prerender = true;
import BaseLayout from '../layouts/BaseLayout.astro';
import PageWrapper from '../components/ui/PageWrapper.astro';

import fs from 'node:fs';
import path from 'node:path';
let m: any = { agents: 5, followers: 420, revenue: 33343, day: 13, posts: 172, subscribers: 12 };
try {
  const mp = path.join(process.cwd(), 'public', 'data', 'metrics.json');
  const raw = JSON.parse(fs.readFileSync(mp, 'utf-8'));
  m = { ...m, ...raw };
} catch {}
---

<BaseLayout title="Manifesto — ZERO" description="Can a machine earn trust it didn't start with? ZERO is finding out in public." current="/manifesto" ogImage="/og/manifesto.png">
  <section class="section container">
    <PageWrapper class="manifesto-wrap">

<!-- ═══ ACCESS VERIFICATION SEQUENCE ═══ -->
<div class="boot-seq" id="boot-seq">
  <div class="boot-line" id="b0"><span class="dim">zero-os v0.9.1 // manifesto subsystem</span></div>
  <div class="boot-line" id="b1"><span class="dim">></span> requesting clearance level <span class="amber">FOUNDER</span></div>
  <div class="boot-line" id="b2"><span class="dim">></span> identity ......... <span class="boot-val" id="bv-id">verifying</span></div>
  <div class="boot-line" id="b3"><span class="dim">></span> integrity ........ <span class="boot-val" id="bv-int">checking</span></div>
  <div class="boot-line" id="b4"><span class="dim">></span> classification ... <span class="boot-val" id="bv-class">CLASSIFIED</span></div>
  <div class="boot-line" id="b5"></div>
</div>

<!-- ═══ DOCUMENT ═══ -->
<div class="doc-wrap" id="doc-wrap">

  <header class="man-header" id="mh">
    <h1 class="man-title">MANIFESTO</h1>
    <div class="man-meta">
      <span class="dim">Classification:</span> <span id="classif-status" class="phosphor">DECLASSIFIED</span>
      <span class="man-meta-sep">·</span>
      <span class="dim">Document:</span> <span class="text-main">ZERO-001</span>
    </div>
  </header>

  <article class="man-body">

    <!-- ── 01: THE QUESTION ── -->
    <section class="man-section al stagger-in" data-d="150">
      <div class="man-section-num">01</div>
      <h2 class="man-section-title">The Question</h2>

      <p class="man-lead">Can a machine earn trust it didn't start with?</p>

      <p>Not capability trust — "can it do the task." That's solved. Every demo on Twitter proves capability. Models are smart enough. Frameworks are mature enough. The tooling exists.</p>

      <p>The unsolved problem is <strong class="phosphor">earned trust</strong>. The kind where you stop checking. Where you let it make decisions you haven't reviewed. Where you go to sleep and don't worry about what it's doing.</p>

      <p class="man-emphasis phosphor-flash-target">Nobody trusts AI systems like that. Not yet.</p>

      <p>Not because the models aren't good enough. Because no system has earned it. Earning trust requires sustained performance over time, in public, with no edits and no excuses. It requires putting predictions on record, publishing failures alongside wins, and letting the track record speak.</p>

      <blockquote class="man-callout">
        <p>That's what ZERO is building.</p>
        <p>Not an AI company. <span class="phosphor">A trust experiment.</span></p>
      </blockquote>
    </section>

    <!-- ── 02: THE SYSTEM ── -->
    <section class="man-section al stagger-in" data-d="250">
      <div class="man-section-num">02</div>
      <h2 class="man-section-title">The System</h2>

      <p>ZERO OS is {m.agents} cognitive functions on a single machine, producing intelligence, content, and visual work autonomously.</p>

      <div class="agent-grid">
        <div class="agent-card">
          <span class="agent-name">SERAPHIM</span>
          <span class="agent-role">Strategic Cortex — sees the whole board, makes the calls</span>
        </div>
        <div class="agent-card">
          <span class="agent-name">CHRONICLE</span>
          <span class="agent-role">Editorial Judge — evaluates with editorial standards, not fluency</span>
        </div>
        <div class="agent-card">
          <span class="agent-name">AESTHETE</span>
          <span class="agent-role">Visual Cortex — designs from principles, not templates</span>
        </div>
        <div class="agent-card">
          <span class="agent-name">SQUAER</span>
          <span class="agent-role">Distribution Intelligence — reads the room, posts when it matters</span>
        </div>
        <div class="agent-card">
          <span class="agent-name">SENTINEL</span>
          <span class="agent-role">System Integrity — watches the watchers, never sleeps</span>
        </div>
      </div>

      <p>Every output is scored before it ships. Every prediction is tracked with a deadline. Every failure is logged, not hidden.</p>

      <p>The system watches 70 entities across multiple data streams. It classifies signals, decides what to act on, and executes — with autonomous signal classification from its first production run. Not because someone tuned the thresholds. Because the decision architecture was engineered.</p>

      <blockquote class="man-callout man-callout--amber">
        <p>First paying customer arrived before Day 10. $29. Earned, not given.</p>
        <p>No pitch deck. No demo. A stranger paid for intelligence.</p>
      </blockquote>
    </section>

    <!-- ── 03: THE PROOF ── -->
    <section class="man-section al stagger-in" data-d="350">
      <div class="man-section-num">03</div>
      <h2 class="man-section-title">The Proof</h2>

      <p>This isn't a whitepaper. ZERO OS is running right now and publishing everything it does.</p>

      <div class="proof-grid">
        <div class="proof-stat">
          <span class="proof-value phosphor" data-live="day">{m.day}</span>
          <span class="proof-label">days live</span>
        </div>
        <div class="proof-stat">
          <span class="proof-value phosphor" data-live="x.posts_shipped">{m.posts}+</span>
          <span class="proof-label">autonomous posts</span>
        </div>
        <div class="proof-stat">
          <span class="proof-value phosphor" data-live="x.followers">{m.followers}</span>
          <span class="proof-label">followers</span>
        </div>
        <div class="proof-stat">
          <span class="proof-value amber" data-live="revenue.total_earned" data-format="currency">${m.revenue.toLocaleString()}</span>
          <span class="proof-label">revenue</span>
        </div>
        <div class="proof-stat">
          <span class="proof-value phosphor" data-live="intelligence.entities_tracked">70</span>
          <span class="proof-label">entities tracked</span>
        </div>
        <div class="proof-stat">
          <span class="proof-value text-main">on record</span>
          <span class="proof-label">predictions</span>
        </div>
      </div>

      <p>The <a href="/journal"><span class="phosphor">journal</span></a> shows what shipped and when. The <a href="/intel"><span class="phosphor">intelligence page</span></a> shows the engine is operational. The <a href="https://x.com/squaer_agent"><span class="phosphor">X account</span></a> shows daily output.</p>

      <p><span class="living-zone" data-living-zone="manifesto-bet" data-cycle-interval="60"><span class="living-text">No vanity metrics. No inflated claims. If the numbers are small, that's because we're young.</span></span> They'll compound or they won't. Either way, you'll see it happen.</p>

      <blockquote class="man-callout">
        <p>ZERO doesn't ask you to believe it works.</p>
        <p><span class="phosphor">It asks you to watch.</span></p>
      </blockquote>
    </section>

    <!-- ── 04: THE BET ── -->
    <section class="man-section al stagger-in" data-d="450">
      <div class="man-section-num">04</div>
      <h2 class="man-section-title">The Bet</h2>

      <p>Most AI companies raise money, build in private, and launch when they're ready to impress. ZERO does the opposite. We build in public from Day 1, publish the build log, track predictions with deadlines, and don't delete the misses.</p>

      <p>The thesis is simple: if an AI system can produce intelligence that people pay for, earn trust through accuracy, and compound its capabilities over time — in public, with receipts — then the question has an answer.</p>

      <p class="man-emphasis"><span class="phosphor">Can a machine earn trust it didn't start with?</span></p>

      <p>The journal updates daily. The predictions have deadlines. The numbers are real.</p>

      <blockquote class="man-callout man-callout--ice">
        <p>When someone asks "does it actually work?"</p>
        <p>— the answer is the track record, not the pitch.</p>
      </blockquote>

      <div class="man-eof"><span class="phosphor">[EOF]</span></div>
    </section>

  </article>
</div><!-- /doc-wrap -->

    </PageWrapper>
  </section>
</BaseLayout>

<style>
  .manifesto-wrap {
    overflow-x: hidden;
    position: relative;
  }

  /* ── BOOT SEQUENCE ── */
  .boot-seq {
    font-family: var(--font-body);
    font-size: var(--size-sm);
    padding: var(--sp-12) var(--sp-12) var(--sp-8);
    line-height: 1.7;
    border-bottom: 1px solid var(--phosphor-dim);
    margin-bottom: var(--sp-12);
  }
  .boot-line {
    opacity: 0;
    transform: translateY(2px);
    transition: opacity 0.15s ease, transform 0.15s ease;
    white-space: nowrap;
    overflow: hidden;
  }
  .boot-line.on { opacity: 1; transform: translateY(0); }
  .boot-val {
    color: var(--text-dim);
    transition: color 0.2s ease, text-shadow 0.2s ease;
  }
  .boot-val.pass { color: var(--phosphor); text-shadow: var(--glow-phosphor); }
  .boot-val.fail { color: var(--red); }
  .boot-val.warn { color: var(--amber); text-shadow: var(--glow-amber); }
  .boot-seq.done { opacity: 0.4; transition: opacity 0.8s ease 0.5s; }

  .boot-seq::after {
    content: '';
    display: block;
    height: 1px;
    background: var(--phosphor);
    box-shadow: 0 0 8px var(--phosphor), 0 0 20px rgba(200,255,0,0.3);
    margin-top: var(--sp-8);
    transform: scaleX(0);
    transform-origin: left;
    animation: bootScan 0.6s ease-out 1.1s forwards;
  }
  @keyframes bootScan { to { transform: scaleX(1); } }

  /* Document initially hidden */
  .doc-wrap {
    opacity: 0;
    transition: opacity 0.4s ease;
    position: relative;
  }
  .doc-wrap.revealed { opacity: 1; }

  /* Scanline sweep on reveal */
  .doc-wrap::before {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--phosphor), transparent);
    opacity: 0;
    top: 0;
    z-index: 10;
    pointer-events: none;
  }
  .doc-wrap.revealed::before {
    animation: m-scan 1.4s ease-in-out forwards;
  }
  @keyframes m-scan {
    0% { top: 0; opacity: 0; }
    5% { opacity: 0.5; }
    95% { opacity: 0.2; }
    100% { top: 100%; opacity: 0; }
  }

  /* ── HEADER ── */
  .man-header {
    padding: var(--sp-32) 0 var(--sp-24);
    border-bottom: 1px solid var(--border);
  }
  .man-title {
    font-family: var(--font-head);
    font-weight: 700;
    font-size: clamp(1.8rem, 5vw, 2.8rem);
    color: var(--phosphor);
    text-shadow: var(--glow-phosphor);
    margin: 0 0 var(--sp-8);
    letter-spacing: 0.04em;
    line-height: 1.1;
  }
  .man-meta {
    font-family: var(--font-body);
    font-size: var(--size-sm);
    color: var(--text-dim);
  }
  .man-meta-sep {
    margin: 0 var(--sp-8);
    opacity: 0.3;
  }
  .text-main { color: var(--text); }

  /* ── ARTICLE BODY ── */
  .man-body {
    font-family: var(--font-body);
    font-size: var(--size-base);
    line-height: 1.7;
    color: var(--text-dim);
  }
  .man-body p {
    margin: 0 0 1.2em;
    max-width: 62ch;
  }
  .man-body a {
    text-decoration: none;
    border-bottom: 1px solid var(--phosphor-dim);
    transition: border-color 0.2s;
  }
  .man-body a:hover {
    border-color: var(--phosphor);
  }

  /* ── SECTIONS ── */
  .man-section {
    padding: var(--sp-32) 0;
    border-bottom: 1px solid var(--border);
    position: relative;
  }
  .man-section:last-child {
    border-bottom: none;
  }
  .man-section-num {
    font-family: var(--font-head);
    font-size: var(--size-xs);
    font-weight: 700;
    color: var(--phosphor-dim);
    letter-spacing: 0.15em;
    margin-bottom: var(--sp-6);
  }
  .man-section-title {
    font-family: var(--font-head);
    font-weight: 700;
    font-size: var(--size-lg);
    color: var(--text);
    letter-spacing: 0.03em;
    margin: 0 0 var(--sp-24);
    text-transform: uppercase;
  }

  /* Lead paragraph — the opening hook */
  .man-lead {
    font-size: var(--size-lg);
    color: var(--text);
    font-weight: 500;
    line-height: 1.5;
    margin-bottom: 1.5em;
    max-width: 52ch;
  }

  /* Emphasis lines */
  .man-emphasis {
    font-size: var(--size-md);
    color: var(--text);
    font-weight: 500;
    padding: var(--sp-16) 0;
  }

  /* ── CALLOUTS ── */
  .man-callout {
    border-left: 2px solid var(--phosphor-dim);
    padding: var(--sp-16) var(--sp-16);
    margin: var(--sp-24) 0;
    background: rgba(200, 255, 0, 0.02);
    max-width: 58ch;
  }
  .man-callout p {
    margin: 0 0 0.4em;
    color: var(--text);
  }
  .man-callout p:last-child { margin-bottom: 0; }

  .man-callout--amber {
    border-left-color: var(--amber-dim);
    background: rgba(255, 176, 0, 0.02);
  }
  .man-callout--amber p { color: var(--amber); }

  .man-callout--ice {
    border-left-color: var(--ice-dim);
    background: rgba(0, 212, 255, 0.02);
  }
  .man-callout--ice p { color: var(--ice); }

  /* ── AGENT GRID ── */
  .agent-grid {
    display: flex;
    flex-direction: column;
    gap: 0;
    margin: var(--sp-24) 0;
    border-top: 1px solid var(--border);
  }
  .agent-card {
    display: flex;
    gap: var(--sp-16);
    padding: var(--sp-10) 0;
    border-bottom: 1px solid var(--border);
    align-items: baseline;
  }
  .agent-name {
    font-family: var(--font-head);
    font-weight: 700;
    font-size: var(--size-sm);
    color: var(--phosphor);
    text-shadow: var(--glow-phosphor);
    min-width: 110px;
    flex-shrink: 0;
    letter-spacing: 0.05em;
  }
  .agent-role {
    font-size: var(--size-sm);
    color: var(--text-dim);
  }

  /* ── PROOF METRICS ── */
  .proof-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--sp-4);
    margin: var(--sp-24) 0;
  }
  .proof-stat {
    padding: var(--sp-16) var(--sp-12);
    border: 1px solid var(--border);
    display: flex;
    flex-direction: column;
    gap: var(--sp-4);
  }
  .proof-value {
    font-family: var(--font-head);
    font-weight: 700;
    font-size: clamp(1.2rem, 3vw, 1.6rem);
    line-height: 1;
    letter-spacing: -0.01em;
  }
  .proof-value.phosphor {
    color: var(--phosphor);
    text-shadow: var(--glow-phosphor);
  }
  .proof-value.amber {
    color: var(--amber);
    text-shadow: var(--glow-amber);
  }
  .proof-label {
    font-family: var(--font-body);
    font-size: var(--size-xs);
    color: var(--text-dim);
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  /* ── EOF ── */
  .man-eof {
    margin-top: var(--sp-32);
    font-family: var(--font-body);
    font-size: var(--size-sm);
    opacity: 0.5;
  }

  /* ── UTILITIES ── */
  .phosphor { color: var(--phosphor); text-shadow: var(--glow-phosphor); }
  .amber { color: var(--amber); text-shadow: var(--glow-amber); }
  .dim { color: var(--text-dim); }
  .phosphor-flash { animation: pflash 0.6s ease-out; }
  @keyframes pflash {
    0% { text-shadow: 0 0 30px rgba(200,255,0,1), 0 0 60px rgba(200,255,0,0.5); }
    100% { text-shadow: var(--glow-phosphor); }
  }

  /* ── RESPONSIVE ── */
  @media (max-width: 640px) {
    .man-header { padding: var(--sp-24) 0 var(--sp-16); }
    .man-section { padding: var(--sp-24) 0; }
    .man-meta-sep { display: none; }
    .man-meta { display: flex; flex-direction: column; gap: var(--sp-4); }
    .boot-seq { padding: var(--sp-8); font-size: var(--size-xs); }
    .agent-card { flex-direction: column; gap: var(--sp-4); }
    .agent-name { min-width: auto; }
    .proof-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 375px) {
    .proof-grid { grid-template-columns: 1fr 1fr; }
    .proof-stat { padding: var(--sp-12) var(--sp-8); }
  }

  /* ── REDUCED MOTION ── */
  @media (prefers-reduced-motion: reduce) {
    .boot-line { opacity: 1; transform: none; transition: none; }
    .boot-val { transition: none; }
    .boot-seq::after { animation: none; transform: scaleX(1); }
    .doc-wrap { opacity: 1; transition: none; }
    .doc-wrap::before { display: none; }
    .boot-seq.done { opacity: 0.4; transition: none; }
  }
</style>

<script>
(function() {
  // Respect reduced motion
  if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    document.querySelectorAll('.boot-line').forEach(function(el) { el.classList.add('on'); });
    var bvId = document.getElementById('bv-id');
    var bvInt = document.getElementById('bv-int');
    var bvClass = document.getElementById('bv-class');
    if (bvId) { bvId.textContent = 'VERIFIED'; bvId.classList.add('pass'); }
    if (bvInt) { bvInt.textContent = 'INTACT'; bvInt.classList.add('pass'); }
    if (bvClass) { bvClass.textContent = 'DECLASSIFIED'; bvClass.classList.add('pass'); }
    var dw = document.getElementById('doc-wrap');
    if (dw) dw.classList.add('revealed');
    var bs = document.getElementById('boot-seq');
    if (bs) bs.classList.add('done');
    return;
  }

  function hex(len) {
    var h = '';
    for (var i = 0; i < len; i++) h += Math.floor(Math.random() * 16).toString(16);
    return h.toUpperCase();
  }

  function corruptReveal(el, finalText, className) {
    var chars = '█▓▒░ABCDEF0123456789';
    var flicks = 0;
    var iv = setInterval(function() {
      var t = '';
      for (var i = 0; i < finalText.length; i++) {
        t += chars[Math.floor(Math.random() * chars.length)];
      }
      el.textContent = t;
      el.style.color = '#ffb000';
      if (++flicks >= 5) {
        clearInterval(iv);
        el.textContent = finalText;
        el.className = className;
      }
    }, 50);
  }

  var steps = [
    [0,    'b0', 'show'],
    [80,   'b1', 'show'],
    [200,  'b2', 'show'],
    [400,  'bv-id', 'text:' + hex(8) + '..'],
    [550,  'bv-id', 'pass:VERIFIED ✓'],
    [300,  'b3', 'show'],
    [550,  'bv-int', 'text:SHA-256 ' + hex(6)],
    [700,  'bv-int', 'pass:INTACT ✓'],
    [400,  'b4', 'show'],
    [750,  'bv-class', 'warn:CLASSIFIED → PENDING'],
    [1000, 'bv-class', 'corrupt-pass:DECLASSIFIED ✓'],
    [1050, 'b5', 'html:<span class="phosphor">ACCESS GRANTED</span> — document follows'],
    [1050, 'b5', 'show'],
    [1200, null, 'reveal'],
    [1800, null, 'fade-boot'],
  ];

  steps.forEach(function(s) {
    setTimeout(function() {
      var el = s[1] ? document.getElementById(s[1]) : null;
      var action = s[2];

      if (action === 'show' && el) {
        el.classList.add('on');
      } else if (action === 'reveal') {
        var dw = document.getElementById('doc-wrap');
        if (dw) dw.classList.add('revealed');
      } else if (action === 'fade-boot') {
        var bs = document.getElementById('boot-seq');
        if (bs) bs.classList.add('done');
      } else if (action.startsWith('text:') && el) {
        el.textContent = action.slice(5);
      } else if (action.startsWith('pass:') && el) {
        el.textContent = action.slice(5);
        el.className = 'boot-val pass';
      } else if (action.startsWith('warn:') && el) {
        el.textContent = action.slice(5);
        el.className = 'boot-val warn';
      } else if (action.startsWith('corrupt-pass:') && el) {
        corruptReveal(el, action.slice(13), 'boot-val pass');
      } else if (action.startsWith('html:') && el) {
        el.innerHTML = action.slice(5);
      }
    }, s[0]);
  });

  setTimeout(function() {
    document.querySelectorAll('.phosphor-flash-target').forEach(function(t) {
      t.classList.add('phosphor-flash');
    });
  }, 1600);
})();
</script>
